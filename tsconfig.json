{
  "extends": "@tsconfig/node20/tsconfig.json",
  "compilerOptions": {
    "module": "node16",
    "declaration": true,
    "outDir": "lib",
    // Symbol.dispose, so `using` works with browsers and advertisements
    "lib": ["ES2023", "ESNext.Disposable"],
  },
  "exclude": ["lib"]
}
